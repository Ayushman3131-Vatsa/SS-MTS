# Optional public product label shown in the UI.
VITE_APP_NAME=Workspace

# Keep this as /api when the frontend and backend share an origin.
VITE_API_BASE_URL=/api

# Must match the backend CSRF_COOKIE_NAME setting.
VITE_CSRF_COOKIE_NAME=mt_csrf
